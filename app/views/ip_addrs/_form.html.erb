<%= form_for(@ip_addr) do |f| %>
  <% if @ip_addr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip_addr.errors.count, "error") %> prohibited this ip_addr from being saved:</h2>

      <ul>
      <% @ip_addr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :addr %><br>
    <%= f.text_field :addr %>
  </div>
  <div class="field">
    <%= f.label :default %><br>
    <%= f.check_box :default %>
  </div>
  <div class="field">
    <%= f.label :server_id %><br>
    <%= f.text_field :server_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
